import 'package:flutter/material.dart';

class SendAndRequestScreen extends StatelessWidget {
  const SendAndRequestScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Send and Request');
  }
}
